# Star-systems

Quick viewer for OpenStreetMap.

## Usage

1. Serve the project root (for example: `python -m http.server 8000`).
2. Open `http://localhost:8000` in your browser.
3. Pan and zoom the map as needed.

Note: The viewer loads tiles from OpenStreetMap, so an internet connection is required.
